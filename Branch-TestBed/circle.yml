test:
  override:
    - xctool -scheme "Scheme 1" -workspace Branch-TestBed.xcworkspace -sdk iphonesimulator test - xctool -scheme "Scheme 2" -workspace Branch-TestBed.xcworkspace -sdk iphonesimulator test - xctool -scheme "Scheme 3" -workspace Branch-TestBed.xcworkspace -sdk iphonesimulator test

